name: Sync Fork

on:
  schedule:
    - cron: '0 2 * * *'

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
    - uses: tgymnich/fork-sync@v1
      with:
        owner: eclipse-cyclonedds
        repo: cyclonedds
        token: ${{ secrets.GITHUB_TOKEN }}
